Data_Structures
===============

Search algorithms for binary trees and graphs

binary_tree.rb - Implementation of a 'breadth first search' and 'depth first search' algorithms for a binary tree.

knight.rb - Use of a breadth first search algorithm to determine the shortest path between any two squares on a chess board that a knight can take.
